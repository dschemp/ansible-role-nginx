---
nginx_daemon_name: "nginx"

nginx_user: "nginx"
nginx_group: "nginx"

nginx_conf_dir: "/etc/nginx"
nginx_confd_dir: "{{ nginx_conf_dir }}/conf.d"
nginx_sites_dir: "{{ nginx_conf_dir }}/sites-enabled"
nginx_log_dir: "/var/log/nginx"

__nginx_system_packages:
  - "nginx"
