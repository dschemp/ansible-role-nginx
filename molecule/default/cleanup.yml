---
- name: "Clean TLS server certificate."
  hosts: "localhost"
  connection: "local"
  gather_facts: false
  tasks:
    - name: "Delete TLS server files dir."
      ansible.builtin.file:
        path: "{{ playbook_dir }}/files/tls"
        state: "absent"
